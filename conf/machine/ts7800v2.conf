include conf/machine/include/tsa38x.inc

KERNEL_DEVICETREE = "marvell/armada-385-ts7800-v2.dtb"

# The idleinject tool is needed to ensure the CPU stays at safe temperatures,
# therefore it is included as part of the machine configuration.
IMAGE_INSTALL:append = " idleinject"
