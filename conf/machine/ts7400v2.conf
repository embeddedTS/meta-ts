include conf/machine/include/tsimx28.inc

KERNEL_DEVICETREE = "nxp/mxs/imx28-ts7400v2.dtb"

WKS_FILE = "tsimx28.wks"

# Older platforms which install a binary U-Boot blob to the target need to use
# WIC to install it.
do_image_wic[depends] += "ts7400v2-uboot:do_deploy"
